# Backend API base URL (FastAPI)
VITE_API_URL=http://localhost:8000

# LiveKit server URL (hardcoded for the UI — the join form no longer collects it)
VITE_LIVEKIT_URL=wss://livekit.srv1240507.hstgr.cloud
